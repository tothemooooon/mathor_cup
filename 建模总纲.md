# MathorCup 2026 A题建模总纲（项目记忆）

> 最新完整版本请优先阅读：`建模全说明.md`（包含 Q1~Q4 当前最优主线、实验设置、结果与代码原理）。

## 0. 项目纲领（记忆锚点）

### 0.1 核心目标
- 目标不是“做出一个能跑的模型”，而是“在竞赛评分规则下最大化总分”。
- 每一个建模与工程决策都要回答三个问题：
  1. 是否提升可解释性（评委能快速看懂并认可）
  2. 是否体现方法专业性（数学建模深度与量子/QUBO契合）
  3. 是否保持代码简洁可复现（提交前稳定复跑）

### 0.2 评分导向权重（固定）
- 可解释性 40%
- 方法专业性 40%
- 代码简洁性 20%

### 0.3 统一决策原则
- 原则 A：先可行，再最优。不可行方案不进入主线。
- 原则 B：先 baseline，再创新。每一问先建立可复现 baseline，再做改进。
- 原则 C：改动必须可追溯。每次改动必须写明“原因-动作-结果-去留”。
- 原则 D：以统一结果契约记录实验，避免“口头最优”。

---

## 1. 环境治理与运行纪律

### 1.1 环境约束（严格执行）
- Python 版本固定：3.10（当前已验证 3.10.20）
- 包管理固定：`uv`
- 虚拟环境固定：项目内 `.venv`
- 禁止：系统 `pip`、全局依赖安装、跨环境混用

### 1.2 依赖管理策略
- 依赖声明：`pyproject.toml`
- 本地 SDK 源：`kaiwu-1.3.1-cp310-none-any.whl`
- 运行命令统一：
  - `source .venv/bin/activate`
  - `python run_baseline.py ...`

### 1.3 复现实验要求
- 每次实验至少记录：题号、参数、随机种子、运行时长、可行性、目标值。
- 结果必须写入 `experiments/results/*.json`。

---

## 2. 评委视角题目解读（提分导向）

### 2.1 评委关心的不是“是否复杂”，而是“是否完整”
- A题本质是 VRP 的分层难化：
  - Q1：单车 TSP（无时间窗、无容量）
  - Q2：单车 + 时间窗惩罚
  - Q3：大规模单车（可解性挑战）
  - Q4：多车 + 容量 + 时间窗 + 车辆数权衡
- 评委期待看到“从小到大、从简到繁”的连续技术路线，而非四问割裂。

### 2.2 可以超越多数选手的关键点
- 关键点 1：统一建模母体
  - 说明四问如何共享变量定义、目标拆分和惩罚逻辑。
- 关键点 2：规模可解性论证
  - Q3 不能只给结果，必须解释为何需要分解、分解损失是什么、如何补偿。
- 关键点 3：权重与参数的可解释调参
  - 展示惩罚系数、车辆权重等参数对结果的影响，不做“黑箱拍脑袋”。
- 关键点 4：真机叙事闭环
  - 即使主求解在模拟退火，也要规划真机尝试记录与哈密顿量曲线展示路径。

### 2.3 常见失分点预警
- 只给最优路径，不给约束校验。
- 只给最终参数，不解释参数选择过程。
- Q3/Q4 只堆算法，不解释复杂度与可复现性。
- 文档与代码不一致，结果不可复跑。

---

## 3. 数据集说明与质量评估

### 3.1 数据表结构
- 表 1：`节点属性信息`
  - 字段：`节点ID`、`开始服务时间下界`、`开始服务时间上界`、`服务时间`、`需求量`、`车容量`
- 表 2：`旅行时间矩阵`
  - 51x51，对应节点 0~50 的旅行时间

### 3.2 字段解释（建模含义）
- `节点ID=0`：仓库（出发与返回点）
- `开始服务时间下界/上界`：时间窗区间
- `服务时间`：客户服务时长
- `需求量`：客户需求，用于容量约束
- `车容量`：车辆载重上限（样例中写于仓库行）

### 3.3 已完成质量检查（2026-04-17）
- 节点表：51 行，字段完整；`Unnamed: 5` 可忽略（空列）
- `车容量`：仅仓库行有效值 60，符合“全车同容量”设定
- 旅行时间矩阵：
  - 维度 51x51
  - 对角线为 0
  - 对称
  - 非负
  - 无缺失

### 3.4 数据风险与处理策略
- 风险：容量字段只在首行出现，直接按列读取会误判缺失。
- 处理：统一从仓库行读取容量，若为空则回退到列非空首值。

---

## 4. Q1-Q4 建模路线（导师视角指导）

### 4.1 Q1（15客户，单车，无时间窗无容量）
- 问题类型：标准 TSP 的 QUBO 表达。
- 变量：`x[i,t]` 表示客户 `i` 是否处于访问序位 `t`。
- 核心难点：
  - 惩罚项过小导致不可行；过大导致目标函数梯度被淹没。
  - 退火随机性导致结果波动。
- 处理方法：
  - 双约束平方惩罚（每位唯一 + 每客唯一）
  - 多 seed 取优 + 固定 seed 复现
- 选择原因：
  - 形式标准、解释清晰、与后续问题可复用。

### 4.2 Q2（15客户，单车，时间窗惩罚）
- 问题类型：TSP + 时间相关惩罚（软约束）。
- 关键思想：将时间窗惩罚直接写入 QUBO 一次项（B），并用迭代修正到达时间估计（C）。
- 难点：
  - 到达时刻与路径强耦合，直接精确入模会引入大量变量。
  - 惩罚估计过粗时，可能导致权重敏感性不足。
- 处理方法：
  - 先估计位置级到达时间 `tau_k`，构造 `penalty(j,k)`。
  - 将 `tw_weight * penalty(j,k) * x[j,k]` 加入目标函数一次项（Q 对角）。
  - 迭代 3-5 轮：用上一轮真实到达时间更新 `tau_k`，重建 QUBO 再求解。
- 选择原因：
  - 变量规模保持 225，不增加二进制变量，工程上可落地且论文叙事完整。

### 4.3 Q3（50客户，单车大规模）
- 问题类型：大规模 QUBO 可解性瓶颈。
- 关键思想：分层混合求解。
  - 先分解子问题（聚类/分片）
  - 子问题内 QUBO 求解
  - 簇间拼接 + 局部改进
- 难点：
  - 分解会损失全局最优性。
- 处理方法：
  - 明确分解准则 + 拼接优化 + 消融验证“分解是否值得”。
- 选择原因：
  - 兼顾“可算性”和“量子求解叙事”。

### 4.4 Q4（50客户，多车+容量+时间窗）
- 问题类型：多目标 VRP（车数 + 路程 + 时间窗违反）。
- 关键思想：两阶段求解。
  - 阶段1：客户分配到车辆（容量可行优先）
  - 阶段2：车内路径优化（QUBO）
- 难点：
  - 车辆数最少与总运输时间最短之间矛盾明显。
- 处理方法：
  - 车辆数扫描，绘制目标变化曲线，给出权重驱动的管理建议。
- 选择原因：
  - 工程上可控，便于解释“决策层含义”。

---

## 5. 人工决策清单（必须你确认）

### 5.1 Q1 决策点
- 惩罚系数主尺度（`lambda_pos`、`lambda_cus`）
- 退火预算（`seed_count`、`iterations_per_t`）

### 5.2 Q2 决策点
- `tw_weight` 网格范围与是否放大量级
- `tau_k` 更新平滑系数 `beta`
- 是否保持“无等待变量”硬约束（默认保持）

### 5.3 Q3 决策点
- 分解粒度（`cluster_size`）
- 是否开启拼接后局部改进（`two_opt`）

### 5.4 Q4 决策点
- 车辆扫描区间（`min_vehicle_count`~`max_vehicle_count`）
- 多目标权重（`vehicle_weight`、`travel_weight`、`tw_weight`）

### 5.5 通用决策点
- 是否每问都提交真机尝试记录（配额允许时建议“是”）

---

## 6. 实验台账（统一模板）

| 时间 | 题号 | 方案 | 关键参数 | 可行性 | 目标值 | 备注 |
|---|---|---|---|---|---|---|
| 2026-04-17 | Q1 | QUBO-TSP + SA | n=15, seed=0, lambda=200/200 | 是 | travel=47 | 已验证可复现 |
| 2026-04-17 | Q2 | 决策驱动批次（tw_weight网格） | weight∈[0,1], seed_count=16, iters=220 | 是 | best: travel=43, tw_pen=164860 | 结果对权重不敏感，路线未切换 |
| 2026-04-17 | Q2 | 候选多样性诊断 | seed_count=120 | 是 | unique pairs=120 | 同时最优 travel 与 tw_pen 出现在同一路线 |
| 2026-04-17 | Q2 | B主线+C迭代（时间窗入模） | rounds=4, tw_grid=[0.1,0.5,1,2,5,10], beta=0.65 | 否（原始解） | objective=162612, travel=42, tw_pen=162570 | 相对旧baseline(241172)下降32.57% |
| 2026-04-17 | Q1 | P 系统调优（A/B两阶段） | A:[2,3,5,8,10]x38@5次, B:[0.5..15]x38@20次 | 是 | 最优P*=19, best=44 | 相对2-opt=33仍高33.33%，未达1.05门槛 |
| 2026-04-17 | Q3 | 决策驱动批次（分解粒度×two_opt） | cluster_size∈{6,8,10,12}, two_opt∈{T,F}, seed_per_cluster=3 | 是 | 推荐: cluster_size=10, two_opt=True | two_opt 显著降低目标值 |
| 2026-04-17 | Q4 | 决策驱动批次（车辆权重×车辆扫描） | vehicle_weight∈{80,120,180}, k∈[5,8], seed_per_vehicle=2 | 是 | 推荐: vehicle_weight=120, selected_k=7 | 当前区间内权重变化不改变最优车辆数 |
| 2026-04-18 | Q3/Q4 | 决策地图复盘（结构层+算法层+参数层） | 策略层全量(Q3:45/Q4:90)+参数层快速扫描(seed=0) | 是 | Q3: best=5,858,011（cluster=8）; Q4: best=96,579（regret+or_opt） | 得到统一最优方案与参数敏感性结论 |

> 记录规则：每次实验都追加，不覆盖历史。
> 对应报告：
> `experiments/results/decision_batch_q2_20260417_143105_report.md`、
> `experiments/results/q2_candidate_diversity_20260417_143506.json`、
> `experiments/results/q2_20260417_160116.json`、
> `experiments/results/q2_iterative_bc_curve_20260417_160114.csv`、
> `experiments/results/q1_penalty_tuning_20260417_164902.json`、
> `experiments/results/q1_penalty_tuning_20260417_164902_paper.md`、
> `experiments/results/decision_batch_q3_20260417_150017_report.md`、
> `experiments/results/decision_batch_q4_20260417_150127_report.md`、
> `experiments/results/q34_decision_map_report_20260418.md`、
> `experiments/results/q34_decision_map_sensitivity_single_seed_20260418_140101_summary.csv`。

---

## 7. 方向变更日志（统一模板）

### 模板
- 时间：
- 变更前：
- 触发原因：
- 变更动作：
- 结果变化：
- 是否保留：

### 已记录
- 时间：2026-04-17
- 变更前：直接按字符串 `x[i][t]` 解析求解输出
- 触发原因：Kaiwu 变量名为零填充格式，导致误解码
- 变更动作：改为按 `str(x_vars[i,t])` 取值
- 结果变化：Q1 可行性从“伪不可行”恢复为可行
- 是否保留：保留

- 时间：2026-04-17
- 变更前：Q2 仅做路径后验重排序（travel + tw_weight * penalty）
- 触发原因：决策批次显示权重变化未触发路线切换，无法体现人工决策价值
- 变更动作：切换到 B+C 主线，将位置级时间窗惩罚一次项写入 QUBO，并进行 4 轮迭代修正
- 结果变化：Q2 objective 从 241172 降到 162612（-32.57%），并输出收敛曲线 CSV/PNG
- 是否保留：保留（作为 Q2 升级主线）

- 时间：2026-04-17
- 变更前：Q3 未明确是否必须进行簇间局部优化
- 触发原因：消融结果显示 `two_opt=False` 时目标值明显恶化（同分解粒度下）
- 变更动作：Q3 主线固定启用 `two_opt`，仅在消融实验中关闭
- 结果变化：形成稳定主线配置 `cluster_size=10, two_opt=True`
- 是否保留：保留

- 时间：2026-04-17
- 变更前：Q4 车辆权重范围未验证对最优车辆数的影响
- 触发原因：批次结果显示 `vehicle_weight∈[80,180]` 下最优 `k` 始终为 7
- 变更动作：后续将扩大权重与时间窗权重联合网格，验证是否存在车辆数切换临界点
- 结果变化：确认当前设置下车辆权重不是主导因素，时间窗惩罚主导目标
- 是否保留：保留（作为 Q4 下一轮实验入口）

- 时间：2026-04-17
- 变更前：计划通过调大/调小 Q1 惩罚系数 P 使 QUBO 逼近经典 2-opt
- 触发原因：两阶段网格（含预算增强）后最优仅到 `44`，与 `2-opt=33` 仍有 `33.33%` 差距
- 变更动作：确认“只调P”不足以过验收线，下一步转向模型结构升级（如边剪枝、候选邻域限制或分段构造）
- 结果变化：得到稳定可行率与明确最优区间 `P*=19`，但验收阈值 `<=1.05` 未满足
- 是否保留：保留（作为Q1升级方向的证据）

---

## 8. 阶段结论与下一步

### 当前阶段结论
- 环境、Q1 baseline、统一运行入口、结果契约已搭建。
- 已具备进入 Q2/Q3/Q4 功能完善与消融实验阶段的工程基础。
- Q2 已完成“时间窗入模”升级（B+C 迭代），相对旧 baseline 目标下降 32.57%。
- 已完成 Q3/Q4 决策批次，获得可执行主线建议：
  - Q3：`cluster_size=10 + two_opt=True`
  - Q4：当前权重区间下 `selected_k=7` 稳定。

### 下一步执行清单
- Step 1：Q1 从“调P”转向“结构升级”（候选边限制/分段构造/混合启发式）以追近 `2-opt×1.05` 门槛。
- Step 2：继续优化 Q2 可行率（提升原始二值解约束满足率），并做 `beta × tw_weight_grid` 稳定性敏感性分析。
- Step 3：Q3 在 `cluster_size=10` 主线下补充 seed 预算敏感性（稳定性 vs 运行时）。
- Step 4：Q4 做联合网格（`vehicle_weight × tw_weight`），寻找车辆数切换阈值。
- Step 5：整理真机调用与哈密顿量曲线展示材料。

---

## 9. Baseline V1 固化（2026-04-17）

### 9.1 固化目标
- 将“实验推荐参数”固化为统一 profile，支持一键复现与参数可追溯。
- 保证 `CLI 显式参数 > profile 参数 > 代码默认值`，兼顾复现与临时试验。

### 9.2 入口与配置
- 统一入口：`run_baseline.py`
- Profile：`configs/baseline_v1.json`
- 一键命令：
  - `python run_baseline.py --profile baseline_v1 --question Q1`
  - `python run_baseline.py --profile baseline_v1 --question Q2`
  - `python run_baseline.py --profile baseline_v1 --question Q3`
  - `python run_baseline.py --profile baseline_v1 --question Q4`

### 9.3 Baseline V1 参数冻结
- Q1：`n_customers=15, seed_start=0, seed_count=8`
- Q2：`mode=iterative_bc, n_customers=15, seed_start=0, seed_count=12, min_rounds=3, max_rounds=5, tw_weight_grid=[0.1,0.5,1,2,5,10], beta=0.65, M1=10, M2=20`
- Q3：`n_customers=50, cluster_size=10, seed_count_per_cluster=3, tw_weight=1.0, do_two_opt=True`
- Q4：`n_customers=50, vehicle_weight=120, tw_weight=1.0, travel_weight=1.0, min_vehicle_count=5, max_vehicle_count=8, seed_count_per_vehicle=2`

### 9.4 Q2 限制说明（本轮冻结）
- 当前 Q2 已将时间窗惩罚入模，但 `tau_k` 仍是估计量，存在估计偏差。
- 当前最优路线对应的原始二值解仍有约束违反，最终提交路线由解码修复得到。
- 下一轮升级方向：提升原始二值可行率并验证不同平滑更新策略。

### 9.5 Baseline V1 建模思路（简明版）
- Q1：标准 QUBO-TSP，使用“位置唯一 + 客户唯一”双惩罚约束。
- Q2：采用“时间窗惩罚一次项直入 QUBO + 自适应惩罚系数 λ”主线，不再使用 B+C 迭代。
- Q3：采用“分片 QUBO + 簇间拼接 + two_opt”处理 50 客户规模瓶颈。
- Q4：采用“两阶段”策略（容量分配 -> 车内QUBO路径），并通过车辆数扫描选取可行最优。

### 9.6 Baseline V1 结果摘要（profile复现）
| 题号 | 参数主线 | 指标摘要 |
|---|---|---|
| Q1 | `n_customers=15` | `travel=47` |
| Q2 | `direct_penalty + adaptive_lambda` | `selected_lambda / adaptive_trace / gap_to_dp` 全链路输出（详见 11 节） |
| Q3 | `cluster_size=10, two_opt=True` | `travel=62, tw_penalty=5425280, objective=5425342` |
| Q4 | `vehicle_weight=120, k∈[5,8]` | `selected_k=7, travel=177, tw_penalty=292790, objective=293807` |

---

## 10. Q3/Q4 提分迭代（Boost V1，2026-04-17）

### 10.1 本轮目标与约束
- 目标：在量子算力限制下提高 Q3/Q4 综合质量，优先保证可行率与稳定性。
- 约束：
  - QUBO 子问题规模上限 `<=15`（硬约束）
  - 统一采用“可行率优先 -> 目标值 -> 运行时”的选择规则
  - 至少 5 个 seed 的全量消融矩阵

### 10.2 消融矩阵与文献映射
- Q3：`分解策略(3) × 路由后处理(3) × seed(5)`，共 45 组。
- Q4：`分配策略(3) × 路由后处理(3) × 车辆扫描模式(2) × seed(5)`，共 90 组。
- 文献驱动主题（5篇）已映射到实验开关：
  - 分解型 VRP
  - 时间窗惩罚建模
  - 量子/退火 QUBO 在 VRP
  - 混合元启发式
  - 多目标车辆数权衡

### 10.3 Boost V1 冻结结论

| 题号 | 推荐策略 | 推荐参数 | 消融结果摘要 |
|---|---|---|---|
| Q3 | `multi_start_fusion + two_opt + tw_repair` | `cluster_size=10, seed_count_per_cluster=2, qubo_cap=15, tw_violation_ratio_cap=0.92` | `feasibility_rate=1.00, objective_mean=6,290,248.4` |
| Q4 | `regret + or_opt + tw_repair + feasibility_filtered` | `k∈[5,8], seed_count_per_vehicle=1, qubo_cap=15, tw_violation_ratio_cap=0.7` | `feasibility_rate=1.00, objective_mean=99,954.2, tw_violation_ratio_mean=0.664` |

### 10.4 关键决策点与理由（评委叙事可直接复用）
- Q3 关键决策：是否采用融合分解。  
  结论：采用 `multi_start_fusion`，原因是可行率与目标值同时领先，且更符合“量子+经典协同”的专业叙事。
- Q4 关键决策：分配层是否使用 `regret`。  
  结论：采用 `regret`，在可行率达标条件下目标均值更优，且对 seed 波动更稳。
- Q4 关键决策：扫描模式 `fixed` vs `feasibility_filtered`。  
  结论：采用 `feasibility_filtered`，在“可行率优先”规则下更稳定。

### 10.5 失败归因（保留为论文讨论）
- Q3/Q4 目标值仍被时间窗惩罚主导，说明在“无等待”设定下时间窗冲突强。
- 当仅做距离导向分解或关闭局部修复时，目标值与可行率明显退化。
- 增加复杂策略但不带来可行率收益的组合，已降级为“消融结论”，不进入主线。

### 10.6 产物与配置落地
- 新增配置：`configs/q3_q4_boost_v1.json`
- 关键报告：
  - `experiments/results/decision_batch_q3_boost_v1_full_20260417_212313_report.md`
  - `experiments/results/decision_batch_q4_boost_v1_full_fix_20260417_214046_report.md`
  - `experiments/results/q3_q4_boost_v1_decision_report_20260417.md`
- 论文可贴表：
  - `experiments/results/decision_batch_q3_boost_v1_full_20260417_212313_paper.md`
  - `experiments/results/decision_batch_q4_boost_v1_full_fix_20260417_214046_paper.md`

---

## 11. Q2 回退与对照实验（2026-04-18）

### 11.1 架构回退动作
- 从 `B+C` 迭代回退到“时间窗惩罚一次项直入 QUBO”。
- 主求解仅保留：`QUBO + Kaiwu SA + 自适应惩罚系数 λ`。
- 统一保留 DP 精确对照，输出 `gap_abs/gap_ratio`。

### 11.2 正式对照设置
- 规模：`n=15`
- 重复：`seed_repeats=3`
- 分组：
  - `QUBO_FIXED(lambda=200)`
  - `QUBO_ADAPTIVE`
  - `BRANCH_CUT(OR-Tools CBC, piecewise TW penalty)`
  - `DP_EXACT`
- 口径：无等待服务（到达即服务）

### 11.3 结果摘要
| Group | best objective | median objective | gap_to_dp(best) | runtime_mean(s) |
|---|---:|---:|---:|---:|
| `QUBO_FIXED` | 214648.0 | 214648.0 | 2.5517 | 19.75 |
| `QUBO_ADAPTIVE` | 207427.0 | 214648.0 | 2.4658 | 45.29 |
| `BRANCH_CUT` | 97920.0 | 97920.0 | 1.1640 | 122.54 |
| `DP_EXACT` | 84121.0 | 84121.0 | 1.0000 | - |

### 11.4 结论与归因
- 回退后的 Q2 主线已可复现，并具备完整质量证据链。
- 自适应 λ 的 `best` 相比固定 λ 改善 `3.36%`，但 `median` 仍持平，说明当前 SA 搜索稳定性仍是瓶颈。
- 分支切割显著优于 QUBO 两组，且接近 DP；当前 QUBO 质量短板主要在搜索效率，而非目标口径缺失。

### 11.5 对应产物
- `experiments/results/decision_batch_q2_compare_20260418_124137.json`
- `experiments/results/decision_batch_q2_compare_20260418_124137.csv`
- `experiments/results/decision_batch_q2_compare_20260418_124137.md`
- `experiments/results/decision_batch_q2_compare_*.md`

---

## 12. Q2 增强版（纯 QUBO-SA）落地（2026-04-18）

### 12.1 本次实现要点
- 搜索层：从单层自适应 `lambda` 升级为二层自适应 `(lambda, tw_weight)`。
- 退火层：支持 3 档退火 profile 集成（`P1/P2/P3`），避免单一档案陷入局部最优。
- 重启层：加入时间窗压力锚点重启（固定首客户），并输出 anchor 贡献统计。
- 表达层：在一元时间窗项基础上新增相邻二元修正项 `x[i,t]x[j,t+1]`，并引入尺度归一化。
- 引导层：若有 DP 精确路径，自动提取边偏置作为小权重 soft bias（不改变题意评估函数）。

### 12.2 新增诊断证据链
- 单次运行输出新增：
  - `selected_lambda`、`selected_tw_weight`、`selected_profile`、`selected_anchor`
  - `best_by_lambda`、`best_by_tw_weight`、`best_by_profile`、`best_by_anchor`
  - `adaptive_trace`（lambda阶段）+ `adaptive_trace_tw_weight`（tw阶段）
  - `profile_contribution`、`anchor_contribution`
  - `search_budget_breakdown`、`qubo_term_scales`
- 对照脚本输出新增：
  - 上述 `selected_*` 分布汇总
  - Branch-Cut 分段数 `pieces` 敏感性表（可选）

### 12.3 运行命令（升级后）
```bash
source .venv/bin/activate
python run_baseline.py --profile baseline_v1 --question Q2

python decision_batch_q2_compare.py --enable-dp --enable-branch-cut \
  --tw-weight-grid 0.8,1.0,1.2 --milp-piece-sensitivity 8,10,12
```

### 12.4 说明
- 题意口径保持不变：无等待服务、`m1=10,m2=20` 二次惩罚。
- 跨方法可比性继续以统一 `objective_eval`（重算后的真实目标）为准。
- 分支切割内部 `objective_milp` 仅用于求解器状态参考，不作为横向比较主指标。

---

## 13. Q3/Q4 决策地图复盘（2026-04-18）

### 13.1 本轮目的
- 按“结构层 -> 算法层 -> 参数层”把第三问与第四问统一拆解，形成可解释的决策闭环。
- 输出“最优方案 + 次优备选 + 参数敏感性”三类证据，避免只报单次最优。

### 13.2 证据来源
- 策略层（全量，多seed）：
  - Q3：`decision_batch_q3_boost_v1_full_20260417_212313.json`（45 runs）
  - Q4：`decision_batch_q4_boost_v1_full_fix_20260417_214046.json`（90 runs）
- 参数层（快速，seed=0）：
  - `q34_decision_map_sensitivity_single_seed_20260418_140101_summary.csv`
  - `q34_decision_map_report_20260418.md`

### 13.3 统一最优方案（当前）
- Q3：
  - 结构层：分解求解（非整体QUBO）
  - 算法层：`multi_start_fusion + two_opt + tw_repair`
  - 参数层：`cluster_size=8`（快速扫描最优），`qubo_cap=15`（与10/12等效，保留实现一致性）
- Q4：
  - 结构层：先分车后路由（两阶段）
  - 算法层：`regret + or_opt + tw_repair + feasibility_filtered`
  - 参数层：`k_range=5-8`、`seed_count_per_vehicle=1`、`qubo_cap=15`（与10/12等效）

### 13.4 关键敏感性结论
- Q3：
  - `cluster_size` 对目标值敏感：`8 < 10 < 12`（本轮 seed=0）
  - `qubo_cap` 在 `10/12/15` 下不敏感（目标一致）
- Q4：
  - `qubo_cap` 在 `10/12/15` 下不敏感（目标一致）
  - `k_range=5-8` 与 `5-9` 持平优于 `6-9`
  - `seed_count_per_vehicle=1` 优于 `2`（当前口径）

### 13.5 风险与后续
- 参数层本轮采用快速单seed扫描，建议下一轮对 `cluster_size=8` 做 5-seed 回归确认后再冻结为正式 profile。
- 当前 Q3/Q4 目标仍由时间窗惩罚主导，后续需继续优化“时间窗可行化”策略以提升评委观感。

---

## 14. 2026-04-19 最新进展（Q4专项提分）

### 14.1 关键升级
- Q4 新增跨车重分配局部搜索（`relocate/swap`），在容量硬约束下做跨车协同优化。
- 车辆扫描新增 `travel_first` 选择模式（可行优先后按 `travel` 选择）。

### 14.2 最优结果
- 当前最优：`travel=109, tw_penalty=51640, objective=51749, feasible=True`
- 结果文件：`experiments/results/q4_20260419_120746.json`
- 对比旧主线（`travel=131`）下降约 `16.79%`，已优于目标参考 `117`。

### 14.3 主线配置（Q4）
- `assignment_strategy=ffd`
- `route_postprocess=or_opt`
- `enable_tw_repair=True`
- `enable_cross_vehicle_refine=True`
- `cross_vehicle_max_iter=16`
- `cross_vehicle_candidate_per_route=4`
- `cross_vehicle_allow_swap=True`
- `vehicle_scan_mode=travel_first`
- `min_vehicle_count=5, max_vehicle_count=14`
- `seed_count_per_vehicle=1`
- `vehicle_weight=0`
